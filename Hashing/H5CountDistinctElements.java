package Hashing;
import java.util.*;
public class H5CountDistinctElements {
    int arr[] = {1,2,3,4,5,6,3,4,5};
    HashSet<Integer> set = new HashSet<>();

    for(int i = 0; i < num.length; i++) {
        set.add(arr[i]);
    }
    System.out.println("ans =" + set.size);
}
